# ============================================================
# requirements.txt — SWE-1 Generator Backend (FastAPI)
# ============================================================

# ----- FastAPI Core -----
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ----- Pydantic & Settings -----
pydantic==2.5.0
pydantic-settings==2.1.0

# ----- Database & Vector Store -----
asyncpg==0.29.0
pgvector==0.3.0
psycopg2-binary==2.9.9
SQLAlchemy==2.0.25

# ----- Supabase -----
supabase==2.9.1

# ----- ElasticSearch (async) -----
elasticsearch[async]==8.11.0

# ----- FAISS (CPU) -----
faiss-cpu==1.7.4

# ----- LangChain -----
langchain-community==0.3.23
langchain-huggingface==0.1.2

# ----- Embeddings & ML -----
sentence-transformers==2.2.2
torch==2.5.1                  # pin to CPU build if GPU not available: torch==2.5.1+cpu

# ----- Ollama Client -----
ollama==0.5.1

# ----- HTTP & Async IO -----
httpx>=0.24.0,<0.25.0
aiohttp==3.12.9
aiofiles==23.2.1

# ----- Configuration & Environment -----
python-dotenv==1.0.0

# ----- NLP -----
nltk==3.8.1

# ----- Excel / CSV / Data -----
pandas==2.1.3
openpyxl==3.1.2
xlsxwriter==3.1.9
numpy==1.25.1

# ----- Resilience / Retry -----
tenacity==9.1.2

# ----- Logging -----
loguru==0.7.2

# ----- Development & Testing -----
pytest==7.4.3
pytest-asyncio==0.21.1
